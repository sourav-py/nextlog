from setuptools import setup, find_packages

setup(
    name='nextlog',
    version='0.1.0',
    description='Nextlog offers developers a customizable and versatile logging experience, allowing them to easily tailor log output formats, destinations, and verbosity levels to suit their specific needs.',
    author='Sourav',
    author_email='imsrv2k@gmail.com',
    packages=find_packages(),
    install_requires=[
        # list of dependencies
    ],
)